sh kill.sh
node index.js

#APP_NAME_DOCKER=$(basename "$PWD")
#sudo docker container rm -f $APP_NAME_DOCKER
#sudo docker build -t $APP_NAME_DOCKER .
#sudo docker run -p 3000:3000 -e ENV='test'  --name $APP_NAME_DOCKER $APP_NAME_DOCKER
